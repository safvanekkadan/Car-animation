import 'package:flutter/material.dart';


final cRadius8 = BorderRadius.circular(8.0);
final cRadius10 = BorderRadius.circular(10);
final cRadius20 = BorderRadius.circular(20);
final cRadius30 = BorderRadius.circular(30);